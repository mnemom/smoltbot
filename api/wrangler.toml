name = "smoltbot-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Custom domain (uncomment when DNS is configured)
# routes = [
#   { pattern = "api.mnemom.ai/*", custom_domain = true }
# ]
